export function transparentIcon(width, height) {
    return {
        sizes: [{ width, height: height !== null && height !== void 0 ? height : width }],
        offset: 0,
        background: false,
        transparent: true,
        rotate: false,
    };
}
export function transparentIcons(...sizes) {
    return {
        sizes: sizes.map((size) => ({ width: size, height: size })),
        offset: 0,
        background: false,
        transparent: true,
        rotate: false,
    };
}
export function opaqueIcon(width, height) {
    return {
        sizes: [{ width, height: height !== null && height !== void 0 ? height : width }],
        offset: 0,
        background: true,
        transparent: false,
        rotate: false,
    };
}
export function maskable(options) {
    return { ...options, purpose: "maskable" };
}
